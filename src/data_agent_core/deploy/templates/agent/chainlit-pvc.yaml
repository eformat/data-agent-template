apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${DOMAIN_NAME}-chainlit-data
  namespace: ${NAMESPACE}
  labels:
    app: ${DOMAIN_NAME}-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: ${CHAINLIT_PVC_SIZE}
