apiVersion: v1
kind: Service
metadata:
  name: ${DOMAIN_NAME}-agent
  namespace: ${NAMESPACE}
  labels:
    app: ${DOMAIN_NAME}-agent
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: ${DOMAIN_NAME}-agent
